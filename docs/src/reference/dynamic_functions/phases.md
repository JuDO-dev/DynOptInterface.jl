```@meta
CurrentModule = DynOptInterface
```

# Phases

```@docs
PhaseIndex
supports_phase
UnsupportedPhase
AddPhaseNotAllowed
add_phase
MOI.is_valid(::MOI.ModelLike, ::PhaseIndex)
InvalidPhaseIndex
```